/show view from lists_controller

- provide(:title, 'Bite List')

%h1.big_title
  =@list.city.abbreviation + " Bite List"

/create a form to post to list_item_path (create method in list_items_controller)
.row{style: 'text-align:center;'}
  .span6.offset3
    /only allow to post when it's the current user's account 
    -if current_user == @list.user
      =form_for :restaurant, as: :post, :url => list_items_path do |f|
        =f.text_field :name, :style => 'width:84%;'
        =f.hidden_field :list_id, value: params[:id]

        =f.submit "Add a Bite", class: 'btn btn-primary', :style => 'height:40px;'

/list all the items in the list
.row
  .span6.offset3
    - @list.list_items.each do |item|
      %ol.lists
        .well
          %h3{style:'float:left;'}
            = link_to item.restaurant.name, '#'
          = link_to "Remove", item, method: :delete, confirm: "Are you sure you want to delete this Bite?", style: 'float:right; font-size:11px; margin-top:-3px;', class: 'btn btn-danger'
